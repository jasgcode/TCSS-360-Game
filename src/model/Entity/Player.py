from src.model.Entity.Entity import Entity


class Player(Entity):
    def __init__(self, position):
        super().__init__(position)
        # Add any additional attributes or methods specific to

